## add custom staleness tests ("freshness") define at the source level, and/or override at table level

version: 2

sources:
  - name: jaffle_shop
    database: raw

    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
        